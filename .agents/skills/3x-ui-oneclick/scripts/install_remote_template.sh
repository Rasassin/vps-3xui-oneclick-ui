#!/usr/bin/env bash
# Template entrypoint for the project skill. The runnable version lives at:
# remote_scripts/install_remote.sh
set -Eeuo pipefail
echo "Copy remote_scripts/install_remote.sh and pass ONECLICK_* environment variables."

